# 紫贻贝壳肉分离智能总控平台发布说明

本目录是可直接发布的静态网站目录，入口文件为 `index.html`。

当前 GitHub Pages 目标网址为 `https://nmb332.github.io/mussel-hmi/`。

搜索引擎能搜索到网页通常需要：

1. 使用公网可访问网址，例如自有域名、GitHub Pages、Vercel、Netlify 或云服务器。
2. 保持页面可匿名访问，不要只放在本机 `127.0.0.1`。
3. 提交站点地图到搜索引擎站长平台，或等待搜索引擎自然抓取。
4. 等待收录生效，通常需要数天到数周。
